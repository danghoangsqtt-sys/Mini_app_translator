*.iml
.gradle
/local.properties
# Keep only the shared XML formatting rules. All machine/user Android Studio
# state is local and must not become part of release history.
/.idea/*
!/.idea/codeStyles/
!/.idea/codeStyles/Project.xml

# Agent/plugin payloads are workstation tooling, not application source.
/.agents/
.DS_Store
/build
/captures
.externalNativeBuild
# JVM crash artifacts / scratch outputs
hs_err_pid*.log
replay_pid*.log
lint_*.txt
verify_run.txt
git_status*.txt
git_diff_stat.txt
