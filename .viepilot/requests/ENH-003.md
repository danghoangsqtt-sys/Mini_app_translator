# 🔧 ENHANCEMENT: Remove dead, leak-prone code (`FileManager`, `EncryptionKey`)

## Meta
- **ID**: ENH-003
- **Type**: Enhancement
- **Status**: new
- **Created**: 2026-09-19
- **Breaking**: No

## Summary
`tools/FileManager.java` (explicitly marked `/**Not used**/` at line 26) opens `FileInputStream`/`ObjectInputStream`/`FileOutputStream`/`ObjectOutputStream` in `getObjectFromFile`/`setObjectInFile` (lines 40, 54) without closing them (no try-with-resources/finally) — a real resource-leak pattern if ever reactivated. `tools/EncryptionKey.java` is likewise dead code, explicitly commented "Not used, this is for an eventually encryption" (line 22).

## Current Feature
Neither class is called from any live code path (dead code).

## Current Behavior
The unused code sits in the codebase as a latent trap for whoever re-enables it without noticing the missing resource cleanup.

## Desired Improvement
Delete both files if genuinely unused, or fix the resource leak and document an actual use if they're meant to be reactivated soon.

## Value Proposition
Removes maintenance confusion and a leak-prone pattern before it's accidentally wired back in.

## Acceptance Criteria
- [ ] Confirm zero call sites via search before deleting
- [ ] Files removed (or leak fixed + justified if kept)

## Implementation Notes
{To be filled}
