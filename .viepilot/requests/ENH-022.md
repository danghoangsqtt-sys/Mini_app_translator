# ENHANCEMENT: Archive obsolete QA worktrees and caches safely

## Meta
- **ID**: ENH-022
- **Type**: Enhancement
- **Status**: new
- **Priority**: Medium
- **Created**: 2026-09-26
- **Reporter**: vp-audit (auto-logged)
- **Assignee**: AI
- **Source**: auto-logged by vp-audit Tier 3
- **Phase**: Repository hygiene; independent of shipping application code

## Summary

The repository root contains retained QA worktrees and caches from earlier audit sessions. They are excluded locally from Git, but consume substantial disk space and make the project directory look polluted.

## Audit evidence

- `.qa-cache/`: 37,191 files, 1,662,325,292 bytes (~1.55 GiB).
- `.worktrees/`: 46,148 files, 823,592,470 bytes (~785 MiB).
- Five detached QA worktrees match `.worktrees/qa-a-*`; four are clean and one contains only JVM crash logs.
- `.worktrees/phase3` is a distinct retained development worktree and must not be grouped into an automatic delete.
- `.gradle/` is an ordinary rebuildable Gradle cache, not one of the QA worktrees.
- `.viepilot/debug/` contains only two small diagnostic files (~4.7 KiB) and is currently the sole untracked path.

## Details

- **Tier**: 3
- **Audit date**: 2026-09-26
- **Affected paths**: `.worktrees/qa-a-*`, `.qa-cache/`, `.viepilot/debug/`

## Desired improvement

Perform a separate, explicitly approved cleanup that verifies exact worktree registrations, branch reachability, and dirty state before archiving/removing only obsolete QA artefacts.

## Acceptance criteria

- [ ] Enumerate exact candidate paths and `git worktree list --porcelain` before removal.
- [ ] Preserve `.worktrees/phase3` unless the maintainer separately approves its removal.
- [ ] Preserve any dirty/unreachable worktree data or export it to a named archive first.
- [ ] Remove obsolete QA worktrees through Git-aware commands before deleting residual directories.
- [ ] Treat `.qa-cache/` and `.gradle/` as separate cache decisions.
- [ ] Verify the main checkout, branches, tags, and `origin/master` are unchanged after cleanup.

## Safety note

This audit did not delete or move any directory. Cleanup is intentionally deferred until the maintainer explicitly authorizes the exact targets.

## Related

- Files: `.worktrees/qa-a-*`; `.qa-cache/`; `.viepilot/debug/`
- Dependencies: local worktree registrations and retained backup branch

## Resolution

—
