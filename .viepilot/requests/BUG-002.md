# 🐛 BUG: Service-account key exposed via unencrypted Android Auto Backup

## Meta
- **ID**: BUG-002
- **Type**: Bug
- **Status**: resolved (Phase 1, task 1.2)
- **Severity**: Critical
- **Created**: 2026-09-19
- **Affected**: `app/src/main/AndroidManifest.xml`, `app/src/main/res/xml/backup_scheme.xml`

## Summary
`AndroidManifest.xml:25-27` sets `android:allowBackup="true"` with `fullBackupContent="@xml/backup_scheme"`, but the backup scheme only excludes an unrelated `AccessTokenLoader.xml` SharedPreferences file (`backup_scheme.xml:19-21`) — it does not exclude `filesDir` (where the service-account key from BUG-001 is stored) or the SharedPreferences key `apiKeyFileName`.

## Actual Behavior
`adb backup` or a cloud backup/restore of the device includes the plaintext GCP service-account key.

## Expected Behavior
`backup_scheme.xml` should explicitly exclude the directory/file holding the service-account key (and any preference key referencing it), or `allowBackup` should be disabled entirely if broader exclusion is impractical.

## Steps to Reproduce
1. Load a GCP key into the app (as in BUG-001).
2. Run `adb backup -f backup.ab nie.translator.rtranslatordevedition` (or trigger a cloud backup).
3. Extract the backup archive — the key file is present.

## Error Messages
N/A.

## Environment
- Version: 1.1.2 (branch `master`)
- Phase: 1 — Security & Stability Hardening

## Acceptance Criteria
- [x] `backup_scheme.xml` excludes the credential storage path (files dir entry + relevant shared-prefs file)
- [ ] Verify on a connected device that backup/restore contains no credential file (Phase 1 exit gate)
- [x] Fixed together with BUG-001 as consecutive Phase 1 tasks

## Fix

The full-backup policy now excludes the complete app-private `file` domain, covering both `gcp_credentials.v1` and any user-named legacy plaintext file before migration. It also excludes the default SharedPreferences file because Android backup XML cannot exclude only the `apiKeyFileName` entry. A JVM regression parses the shipping XML and asserts these exclusions plus the existing access-token preference exclusion.

Verification on 2026-09-20: `testDebugUnitTest`, `lintDebug`, and `assembleDebug` passed. Device backup archive inspection awaits a connected Android device/emulator and remains recorded in the phase exit gate.
