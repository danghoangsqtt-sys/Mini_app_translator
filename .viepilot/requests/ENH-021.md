# ENH: No-key, on-device-first translation architecture

## Meta
- **ID**: ENH-021
- **Type**: Enhancement
- **Status**: in_progress
- **Priority**: Critical
- **Created**: 2026-09-23
- **Reporter**: Product owner
- **Assignee**: TERRA (implementation), PM (scope and QA gate)
- **Phase**: Phase 9 — No-Key On-Device Translation

## Summary

Make Mini Conversation usable without asking ordinary users to create a Google Cloud project, enable billing, or import a service-account JSON file. The default path must use supported Android/Google on-device capabilities; the existing Cloud implementation may remain only as an explicit legacy/advanced option during migration.

## Product decision

- Default: Android SpeechRecognizer + ML Kit Translation + Android TextToSpeech.
- Prefer on-device speech recognition when the device exposes it; use the system recognizer fallback with truthful disclosure when it does not.
- Never scrape `translate.google.com/m` in production.
- Never embed a shared Google Cloud credential in the APK.
- A future managed Cloud backend is optional and outside Phase 9 unless separately approved.

## Acceptance criteria

- [x] First launch does not require a Google Cloud key or billing setup.
- [x] Translation models can be downloaded, used offline, inspected, and deleted.
- [x] Speech-recognition capability is detected without crashing on API 23–36.
- [x] Conversation and WalkieTalkie use engine interfaces instead of constructing Cloud clients directly.
- [x] Missing models, missing recognizers, denied microphone permission, and offline/network failures produce recoverable UI states.
- [x] Legacy Cloud management is explicitly optional/advanced and no service-account key is bundled with the APK.
- [ ] Automated and device gates pass before any release is marked ready.

## Non-goals

- Wi-Fi Hotspot transport.
- Java package/applicationId migration.
- Copying AGPL code from `D:\DataAdmin\vp-pdf`.
- Shipping an undocumented Google Translate web endpoint.

## Audit notes

Re-detected by `/vp-audit` on 2026-09-26:

- The on-device runtime is integrated, but `README.md`, both privacy notices, `.viepilot/PROJECT-CONTEXT.md`, `.viepilot/ARCHITECTURE.md`, and the architecture Mermaid sidecar still describe a Cloud-first product. This is release-blocking documentation/privacy drift owned by Task 9.6.
- `SupportTtsQualityPreference.onBindViewHolder()` still constructs the legacy Cloud `Translator`; its constructor initiates the API-token path even though the field is unused. Opening Settings must not silently touch legacy credentials in the default on-device journey.
- Task 9.6 must not imply that selecting `LegacyCloudEngineFactory` alone enables a supported Cloud runtime: the current legacy speech adapter consumes PCM while the new mode controllers own engine capture. A functional Cloud-mode reintroduction requires a separate architecture decision. For Phase 9, retain credential import/delete only as clearly labeled advanced migration management, with no automatic fallback or hidden runtime selection.

Task 9.6 completed at `8a8882b` on 2026-09-26. Default Settings no longer constructs a legacy translator; EN/IT UI/privacy and current architecture docs describe the implemented on-device behavior; no functional Cloud selector or fallback was added. ENH-021 remains `in_progress` until Task 9.7 closes the physical-device, signing, legal-owner, and release gates.
