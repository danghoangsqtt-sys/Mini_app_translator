# 🐛 BUG: `Tools.merge()` byte-array corruption

## Meta
- **ID**: BUG-003
- **Type**: Bug
- **Status**: new
- **Severity**: High
- **Created**: 2026-09-19
- **Affected**: `tools/Tools.java:294-308` (`merge()`), used by `voice_translation/cloud_apis/voice/Recognizer.java`

## Summary
The inner loop in `merge()` never increments its position counter, so every byte from every input array is written to index 0 of the result buffer, silently corrupting the merged output.

## Actual Behavior
Confirmed by code read: the counter variable used as the write index is not incremented inside the copy loop (lines ~294-308). Every array element overwrites index 0; only the very last byte written survives at position 0, the rest of the returned buffer is left as its default/garbage state.

## Expected Behavior
`merge()` should concatenate all input byte arrays correctly, preserving every byte at its correct sequential offset.

## Steps to Reproduce
1. Trigger a code path in `Recognizer.java` that calls `Tools.merge()` on multiple audio/protocol byte chunks.
2. Inspect the merged result — content beyond index 0 is not what was passed in.

## Error Messages
No crash — silent data corruption, likely surfaces as garbled/failed speech recognition downstream.

## Environment
- Version: 1.1.2 (branch `master`)
- Phase: 1 — Security & Stability Hardening

## Acceptance Criteria
- [ ] `merge()` correctly concatenates arrays (fix the missing counter increment)
- [ ] Unit test added covering multi-array merge with distinct byte patterns per array
- [ ] Confirm no other caller depended on the buggy index-0-only behavior

## Fix
{To be filled when resolved}
