# 🔧 ENHANCEMENT: Investigate unrelated `.agents/skills/` content at repo root

## Meta
- **ID**: ENH-017
- **Type**: Enhancement
- **Status**: resolved on canonical sanitized `master`
- **Created**: 2026-09-19
- **Breaking**: No

## Summary
An untracked `.agents/skills/` directory exists at the repo root containing content unrelated to this Android app: a `pdf-translate` skill, a Python `pdf2zh` module, `agent-knowledge/` docs referencing a different app (PDF translator with OCR/GUI in Python), and copies of various `vp-*` skill definitions.

## Current Feature
N/A — appears to be accidental clutter, not app source.

## Current Behavior
This content sits untracked at the project root; it was excluded from the codebase audit as out-of-scope (it is not RTranslator source code), but its presence is unexplained.

## Desired Improvement
Confirm with the user/maintainer whether this was placed intentionally (e.g. leftover from working on a different project in the same terminal session) and, if not needed, remove it to avoid confusing future contributors or tooling that scans the repo root.

## Value Proposition
Prevents future confusion (including for ViePilot's own brownfield scanner, which correctly ignored it this time but could be misled by it in other tooling).

## Acceptance Criteria
- [x] Maintainer approved preparing a sanitized history without the unrelated payload
- [x] `.agents/skills/` is absent from the candidate tree and entire replayed history
- [x] `/.agents/` is ignored and documented as workstation/plugin tooling

## Implementation Notes
{To be filled — this is a question for the user, not an autonomous fix}

## Discussion

Re-detected by vp-audit 2026-09-23: this directory is no longer merely untracked; `master` tracks 358 files under `.agents/skills/` totaling about 5.3 MB, including unrelated PDF translator source and duplicated skills. Confirm whether this content belongs in the application repository before the next release.

Resolved by Phase 8 Task 8.2: canonical `master` contains zero `.agents/` objects, `.gitignore` prevents recurrence, and the sanitized history is synchronized with `origin/master`.
