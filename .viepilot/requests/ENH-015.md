# 🔧 ENHANCEMENT: Review fork `master` against upstream `v3.00`

## Meta
- **ID**: ENH-015
- **Type**: Enhancement
- **Status**: new
- **Created**: 2026-09-19
- **Breaking**: No (a decision/process issue, not a code change)

## Summary
The locally checked-out branch `master` is valid for this fork and tracks `origin/master`. The separate upstream RTranslator remote defaults to `upstream/v3.00`. Audit findings were gathered against the fork's older v1.x-derived code, so upstream v3 may contain useful fixes or incompatible changes that should be reviewed before duplicating work.

## Current Feature
N/A — process/repository-state issue.

## Current Behavior
Development/audit work targets the user's fork as requested. Its lineage may still lack fixes already present upstream.

## Desired Improvement
Keep the fork branch as the implementation target, compare relevant upstream v3 changes before each large phase, and selectively port only compatible fixes. A wholesale branch switch requires a separate migration decision because the user has requested a rebrand and UI plan for the current fork.

## Value Proposition
Prevents wasted effort fixing bugs on a branch that may already differ substantially from where the project is actually headed.

## Acceptance Criteria
- [x] Current implementation target recorded as fork `origin/master`
- [ ] Compare upstream v3 implementations for each touched subsystem before coding
- [ ] Record which fixes are ported, already present, or rejected as incompatible

## Implementation Notes
Branch ambiguity is resolved. Upstream comparison remains a preflight step, not a blocker to planning.
