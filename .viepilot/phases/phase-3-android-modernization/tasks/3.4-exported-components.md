# Task 3.4 — Declare component export policy

## Request

`BUG-014`; must land with task 3.1.

## Paths

```text
app/src/main/AndroidManifest.xml
app/src/androidTest/
```

## Plan

Set launcher `LoadingActivity` exported true and explicitly keep internal activities/services/providers non-exported unless a verified external integration needs access. Inspect merged manifest from all dependencies, not just the source manifest. Install and launch on API 31+ after the target bump.

## Done when

- Manifest merger succeeds and external component surface is reviewed.
- Launcher works on API 31/34/36; internal components are not unintentionally exported.
- Integrated build/device checks pass with 3.1.

## Entry Contract Refinement

Review every activity, service, and provider in the source manifest and in merged debug and release manifests. `LoadingActivity` is the launcher and must be explicitly exported. Internal components default to `android:exported="false"` unless a concrete external integration proves otherwise.

`GeneralService` is abstract, has no direct application target, and is nevertheless currently declared in the manifest. Task 3.4 must verify that declaration and is expected to remove it if it is stale. This is an export-policy decision only; do not assign it a foreground-service type.

Acceptance evidence includes merged-manifest output for both variants and launcher/device tests, not a source-manifest edit alone.
