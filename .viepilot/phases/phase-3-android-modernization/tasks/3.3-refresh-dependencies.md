# Task 3.3 — Refresh Android/cloud dependencies

## Request

`ENH-012`; after toolchain baseline in 3.1.

## Paths

```text
app/build.gradle
build.gradle
settings.gradle
app/libs/
app/proguard-rules.pro
app/proguard-google-api-client.pro
app/src/main/proto/
app/src/main/java/nie/translator/rtranslatordevedition/database/AppDatabase.java
app/src/test/
app/src/androidTest/
```

## Plan

Inventory direct Maven dependencies, all bundled `app/libs/` JARs, protobuf/protoc/gRPC generator/runtime versions, generated sources, and ProGuard/R8 rules. Confirm current supported versions and migration notes from maintainers at execution time. Upgrade only in this order: Room, Nearby, protobuf/gRPC, Google auth/Translate/JWT, then UI/test dependencies.

Before each next group, capture a dependency report, duplicate-class check, build/JVM test result, and release/R8 checkpoint. Remove no JAR before dependency-graph proof shows its replacement and absence of duplicate classes. Do not assert a vulnerability without an advisory.

## Done when

- Each dependency group has a reproducible dependency report, duplicate-class result, build/JVM result, and release/R8 checkpoint.
- Dependency tree is reproducible from supported repositories and no avoidable duplicate JARs remain after proof.
- Host build/test/lint gates pass and cloud/Bluetooth/device smoke tests remain functional.
- Version choices and any retained legacy dependency risk are documented.
