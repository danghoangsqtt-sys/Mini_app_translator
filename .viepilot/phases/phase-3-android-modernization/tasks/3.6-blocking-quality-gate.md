# Task 3.6 — Make lint and release checks blocking

## Source

2026-09-20 `vp-audit`: debug lint reported 5 errors and 119 warnings while `abortOnError=false`; `assembleRelease` produced an unsigned APK. Coordinate the GraphView findings with 2.6 and gRPC dependency findings with 3.3.

The PM-confirmed Phase 3 entry baseline is 5 errors and 121 warnings: three `ResourceType` errors in `GridLabelRenderer` and two `InvalidPackage` errors from `grpc-core:1.11.0`. The 5/119 result is historical provenance only.

## Paths

```text
app/build.gradle
app/src/main/java/nie/translator/rtranslatordevedition/tools/gui/graph/GridLabelRenderer.java
app/proguard-rules.pro
.viepilot/phases/phase-3-android-modernization/SPEC.md
```

## Plan

Classify each lint error by source and user impact. Fix the three `GridLabelRenderer` resource-type errors and determine whether the two `grpc-core` `InvalidPackage` errors disappear with dependency refresh; otherwise document a narrow, justified disposition. Triage warnings and record an explicit baseline rather than suppressing all. Enable a build-breaking lint gate after errors are cleared. Keep signing credentials external to the repository and document how a signed release is produced; Phase 5.6 validates the artifact on device.

## Done when

- `lintDebug` and release lint/checks have zero unexplained errors and fail CI/build on new errors.
- `testDebugUnitTest assembleDebug assembleRelease` pass, and R8 output is reviewed.
- Release-signing procedure is documented without secrets or committed keystores.

## Entry Contract Refinement

This is the Phase 3 blocking quality gate: compare lint by error ID, source, and count against the 5-error baseline, triage all warnings from the 121-warning baseline, and reject every new lint error. Do not mass-suppress errors or warnings, and do not use a successful Gradle exit code as evidence that lint is clean.

Run debug and release lint/checks, `testDebugUnitTest`, `assembleDebug`, and `assembleRelease`; review the R8 result. Keep signing credentials, keystores, and machine-specific paths out of the repository. A signed distributable and its on-device validation need a separately authorized external-signing/Phase 5 owner.

Task 3.6 must not reopen BUG-012 or represent the Phase 2 SecondScale reachability disposition as a production GraphView fix. This entry-contract commit changes no production GraphView code.
