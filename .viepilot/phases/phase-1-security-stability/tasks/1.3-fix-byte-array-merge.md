# Task 1.3 — Fix byte-array merge corruption

## Objective

Make `Tools.merge` concatenate all input byte arrays in order without overwriting index zero, and lock the behavior with focused boundary tests.

## Request

`BUG-003`

## Paths

```text
app/src/main/java/nie/translator/rtranslatordevedition/tools/Tools.java
app/src/test/java/nie/translator/rtranslatordevedition/tools/ToolsTest.java
.viepilot/requests/BUG-003.md
.viepilot/phases/phase-1-security-stability/PHASE-STATE.md
.viepilot/TRACKER.md
.viepilot/HANDOFF.json
CHANGELOG.md
```

## File-Level Plan

| File | Change | Why |
|---|---|---|
| `Tools.java` | Replace the byte-by-byte loop with offset-based `System.arraycopy` calls and reject total-length overflow. | Preserve every byte, handle empty arrays, and avoid silent length wrap. |
| `ToolsTest.java` | Cover no inputs, empty inputs, a single array, multiple distinct arrays, and source immutability. | Reproduce the defect and protect the method's boundaries. |
| ViePilot state/request/changelog | Record start, verification evidence, status, and completed behavior. | Preserve incremental execution traceability. |

## Verification

```text
gradlew.bat testDebugUnitTest lintDebug assembleDebug
```

Expected:

- `ToolsTest` proves exact byte ordering for multiple arrays.
- Empty and single-array calls return correct independent buffers.
- Build, lint, and unit tests pass.

## Execution Log

- 2026-09-20: Contract created and task marked `in_progress` before shipping-file edits.
- 2026-09-20: Replaced the broken byte loop with checked, offset-based `System.arraycopy` concatenation.
- 2026-09-20: Repository-wide call-site review found no production caller of `Tools.merge`; `Recognizer.headers.merge(...)` is `io.grpc.Metadata.merge` and is unrelated. No caller can depend on the former corrupt behavior.
- 2026-09-20: Added three unit tests covering no inputs, a single independent copy, empty segments, multiple distinct patterns, exact ordering, and source immutability.
- 2026-09-20: `testDebugUnitTest`, `lintDebug`, and `assembleDebug` passed; `ToolsTest` reported 3 tests, 0 failures, 0 errors.
- 2026-09-20: Task completed.
- skills_applied: `[vp-auto@0.2.2]`

## Files Changed

| File | Result |
|---|---|
| `Tools.java` | Corrected concatenation and guarded aggregate length overflow. |
| `ToolsTest.java` | Added focused boundary and ordering regression coverage. |
