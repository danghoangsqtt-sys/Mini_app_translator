# Task 7.4 — Remove the committed JDK path

## Source

`BUG-020`: `gradle.properties` pins Gradle to one developer's Microsoft JDK installation path.

## Paths

```text
gradle.properties
README.md
.viepilot/requests/BUG-020.md
.viepilot/phases/phase-3-android-modernization/SPEC.md
.viepilot/phases/phase-3-android-modernization/tasks/3.1-toolchain-api36.md
```

## Plan

Remove only `org.gradle.java.home` from tracked `gradle.properties`; preserve shared JVM and AndroidX settings. Document portable JDK 17 selection through `JAVA_HOME`, Android Studio's Gradle JDK setting, or CI configuration, with no machine-specific path or secret committed. Run the Gradle quality commands using the selected JDK 17 environment and record actual results. If the host lacks a usable JDK/SDK, report that as an environment blocker rather than restoring the personal path.

## Done when

- `gradle.properties` has no `org.gradle.java.home` setting and no personal filesystem path.
- Project documentation explains portable JDK 17 setup without hardcoding a local installation directory.
- `testDebugUnitTest`, `lintDebug`, and `assembleDebug` pass on a supported JDK 17/Android SDK environment, or the exact unavailable prerequisite is documented.

## Execution evidence

- Removed `org.gradle.java.home`; `gradle.properties` now contains no personal filesystem path or replacement path.
- Added portable JDK 17 setup guidance to `README.md` for `JAVA_HOME`, Android Studio's Gradle JDK setting, and CI configuration.
- Ran `./gradlew.bat clean testDebugUnitTest lintDebug assembleDebug --console=plain` on 2026-09-23: **BUILD SUCCESSFUL** (57 actionable tasks); 47 unit tests passed with 0 failures/errors; lint reported 0 errors and 139 existing warnings; debug APK produced at `app/build/outputs/apk/debug/app-debug.apk`.
