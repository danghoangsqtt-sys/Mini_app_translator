# Phase 8 deep audit — 2026-09-23

## Verdict

The current application baseline builds, tests, installs, and cold-launches successfully on the available Pixel 7a API 36 emulator. It is not release-ready yet because repository persistence, dependency refresh, two-phone Bluetooth/SCO coverage, signed release verification, blocking release lint, and visual/accessibility closure remain open.

## Executed evidence

- `gradlew.bat clean testDebugUnitTest lintDebug assembleDebug connectedDebugAndroidTest --console=plain`: PASS, 85 tasks.
- JVM tests: 47 passed, 0 failed.
- Pixel 7a API 36 instrumentation: 7 passed, 0 failed.
- Debug APK installed; cold launch reached `AccessActivity` in 709 ms with a live process and no crash, ANR, or security exception in the captured log.
- Lint: 0 errors / 139 warnings on the source audit; 0 errors / 138 warnings on the sanitized-candidate rerun. The only delta was the time-dependent `AndroidGradlePluginVersion` advisory for a newer Gradle version, not a source difference.

## Findings and disposition

| Severity | Finding | Disposition |
|---|---|---|
| High | Local source history was 44 commits ahead and included 358 unrelated `.agents/skills` files | Sanitized candidate prepared in Task 8.2; push/canonical switch pending |
| High | Release lint is non-blocking (`checkReleaseBuilds false`, `abortOnError false`) | Existing Phase 8 Task 8.6 |
| Medium | Onboarding presents obsolete Google Cloud `$300`/one-year guidance | Auto-logged as `BUG-021` |
| Low | Onboarding still shows the RTranslator logo | Auto-logged as `BUG-022` |
| Medium | Old dependency chain includes Room 2.1, gRPC 1.11, Google HTTP Client 1.19 and a transitive trust-all lint warning | Existing `ENH-012` / Task 8.4; exploit reachability not established |
| Low | Room schema export directory is not configured | Include in Task 8.4 migration/schema gate |
| Low | AndroidTest manifest `package` is ignored by modern AGP | Clean up with explicit `testNamespace` only if needed |
| Low | Gradle Groovy space-assignment syntax is deprecated toward Gradle 10 | Migrate during build-script maintenance |
| Low | 17 `printStackTrace` sites and 6 `AsyncTask` references remain | Existing `ENH-005` / `ENH-006` |

## Guardrails for subsequent implementation

- Canonical `master`, mapped Phase 5–8.1 tags, and `origin` are now synchronized following explicit approval; retain the local backup branch until release closure.
- Do not delete any worktree until it is clean and separately approved; `qa-a-3b66` is currently dirty.
- Run unit, lint, assemble, and connected tests after every dependency batch; roll back the batch if the baseline regresses.
- Do not suppress the transitive trust-manager warning without either upgrading/removing the dependency or proving the code path unreachable.
- Export and version Room schemas before exercising database migrations.
- Complete two-phone Bluetooth/SCO, TalkBack, screenshot, signed release, and install/upgrade checks before closing Phase 3/5/8.

## Official references checked

- Android lint: https://developer.android.com/studio/write/lint
- Android module namespace/test namespace: https://developer.android.com/build/configure-app-module
- Room migrations and schema export: https://developer.android.com/training/data-storage/room/migrating-db-versions
- Gradle warning modes: https://docs.gradle.org/current/userguide/command_line_interface.html
- Google Cloud free trial: https://docs.cloud.google.com/free/docs/free-cloud-features
- Cloud Translation pricing: https://cloud.google.com/products/translate/pricing
- Speech-to-Text pricing: https://cloud.google.com/speech-to-text/pricing
