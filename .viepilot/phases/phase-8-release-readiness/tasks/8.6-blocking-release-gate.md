# Task 8.6 — Enforce the blocking quality gate and verify a signed release

## Source

Phase 3 Task 3.6 remains partial: lint currently has zero errors but `abortOnError false`; prior release output was unsigned.

## Paths

```text
app/build.gradle
gradle.properties
app/proguard-rules.pro
app/src/test/
app/src/androidTest/
.viepilot/phases/phase-3-android-modernization/tasks/3.6-blocking-quality-gate.md
.viepilot/phases/phase-8-release-readiness/RELEASE-CHECKLIST.md
README.md
CHANGELOG.md
```

## Plan

Replace the permissive lint configuration with a build-breaking zero-error gate without introducing broad suppressions. Run a clean build with unit tests, lint, debug assembly, connected instrumentation and release assembly/R8. Use an external keystore or approved CI secret store; never commit keystore paths, passwords or secrets. Install the signed artifact on representative devices and repeat critical credential, Conversation and WalkieTalkie smoke paths. Record artifact hash, signing certificate fingerprint, commit, commands, test counts, lint counts and known limitations.

## Done when

- A deliberately introduced lint error proves the gate fails, and the clean project returns to zero lint errors.
- The full automated command set passes from a clean checkout on JDK 17.
- A signed release artifact is produced externally, hashed, installed and smoke-tested.
- No signing secret or machine-specific path appears in Git or build logs committed to the repository.
- Phase 3 Task 3.6 is complete.
