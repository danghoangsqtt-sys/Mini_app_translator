# Task 8.2 — Resolve source-of-truth and repository hygiene

## Source

The 2026-09-23 audit found a stale Phase 3 worktree, five detached QA worktrees, 358 tracked files under unrelated `.agents/skills/`, 13 tracked `.idea` files, and 44 local source commits not yet persisted to `origin`.

## Paths

```text
.gitignore
.agents/skills/
.idea/
.viepilot/TRACKER.md
.viepilot/requests/ENH-015.md
.viepilot/requests/ENH-016.md
.viepilot/requests/ENH-017.md
README.md
```

## Plan

Prepare a clean candidate history from `origin/master` without changing the primary `master`. Compare the two unique Phase 3 branch commits with current `master` and document their disposition; do not merge/cherry-pick the branch wholesale. Exclude unrelated `.agents/skills/` payloads and retain only intentionally shared IDE formatting configuration. Inventory detached/stale worktrees and mark them removable only when clean and when their commits remain reachable from a named branch/tag. Do not push, replace `master`, move tags, or delete directories/worktrees without a separate maintainer approval.

## PM decision and local execution — 2026-09-23

- Candidate branch: `codex/phase8-sanitized-history`, based on `origin/master`, in the isolated managed worktree `C:\Users\Admin\.codex\worktrees\phase8-sanitized\Mini_app_Translator`.
- Source history: replayed 44 local commits as 43 clean commits. Commit `3b66260` was omitted because its only purpose was deleting IDE files that the clean replay never introduced.
- Commit `4121be7` retained its Android fixes but excluded four accidental `.idea` files. Commit `29e2156` retained its Mini Conversation/ViePilot documentation but excluded all 358 unrelated `.agents/skills/` files and its local `.idea` changes.
- Validation: the clean range contains zero `.agents/` objects and zero non-baseline local `.idea` objects. The `app/` tree hash is identical on the source and candidate (`2cc69efdbc1fe96c43347e9b029761212522fe4d`). Outside `.agents/` and `.idea/`, the candidate tree has no diff from source `master` before this task's documentation changes.
- Candidate quality gate: `clean testDebugUnitTest lintDebug assembleDebug connectedDebugAndroidTest` passed (85 tasks), with 47/47 JVM tests, 7/7 Pixel 7a API 36 tests, lint 0 errors / 138 warnings, and an 11,186,919-byte debug APK. The source audit reported 139 warnings; the only delta was a time-dependent “newer Gradle available” advisory.
- IDE policy: keep only `.idea/codeStyles/Project.xml` as shared formatting configuration. Untrack all other `.idea` files while leaving local copies on disk; `/.idea/*` is ignored except the shared code-style file.
- Agent policy: `/.agents/` is ignored and absent from the candidate history because it is workstation/plugin payload, not application source.
- Phase 3 worktree: `2443168` only records obsolete state, while `e72d7a2` leaves `targetSdk 29`. Current release code already has the later AGP 8.13.2 / Gradle 8.13 / compile+target SDK 36 / minSdk 23 implementation, so no unique hunk should be ported.
- QA worktrees: `09293b4`, `0b50beff`, `3b66260`, `9ae921f`, and `b3f76d0` are all ancestors of source `master`. Four are clean. `qa-a-3b66` contains four untracked JVM crash/replay logs and is not removable. No worktree was deleted.
- Tag relocation followed the exact old-to-new targets recorded in `8.2-history-map.md`; remote peeled targets were verified after the approved canonical cutover.
- Canonical cutover completed after explicit maintainer approval: `master` and `origin/master` were advanced to the sanitized history, and Phase 5–8.1 tags were recreated at their mapped targets and pushed. The pre-sanitization history remains reachable from local backup branch `codex/backup-master-pre-sanitize-20260923`.

## Done when

- [x] The sanitized candidate is approved, remotely persisted, and replaces `master` as the canonical release source.
- [x] The stale Phase 3 branch has a written disposition; no unique hunk is required.
- [x] `.agents/skills/` and `.idea` each have an explicit policy reflected in Git.
- [x] No unique work is lost; every QA commit is reachable and the dirty worktree is retained.
- [x] The candidate release diff contains no unrelated project payload.
