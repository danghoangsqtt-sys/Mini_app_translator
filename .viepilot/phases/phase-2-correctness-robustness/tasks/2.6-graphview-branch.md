# Task 2.6 — Resolve GraphView second-scale crash branch

## Request

`BUG-012`

## Paths

```text
app/src/main/java/nie/translator/rtranslatordevedition/tools/gui/graph/GridLabelRenderer.java
app/src/main/java/nie/translator/rtranslatordevedition/tools/gui/CustomDayGraphsPagerAdapter.java
app/src/main/java/nie/translator/rtranslatordevedition/tools/gui/graph/SecondScale.java
app/src/test/java/nie/translator/rtranslatordevedition/tools/gui/graph/SecondScaleReachabilityProofTest.java
.viepilot/requests/BUG-012.md
.viepilot/phases/phase-2-correctness-robustness/tasks/2.6-graphview-branch.md
```

## Plan

Trace every application GraphView creation, XML declaration, direct second-scale call, reflection
path, and the vendored public API before changing code. If the throw is unreachable and no API can
switch the manual flag, reclassify it as a future dead-code capability risk; do not remove the
throw or implement an unused auto-bounds algorithm. If a reachable path emerges, create a
fail-before-fix runtime test and then test manual/automatic bounds, empty data, min=max, and
rendering. Keep GraphView lint work in Task 3.6.

## Done when

- Reachability and severity are evidence-backed in `BUG-012`.
- The current application has no reachable `Not yet implemented` chart path; future second-scale
  work must reopen the risk.
- A source-level reachability proof (not runtime reproduction) and host build checks pass.

## Execution log

- Application graph creation: only `CustomDayGraphsPagerAdapter.DateGraph`, declared in
  `component_credit_graph.xml`; it configures the primary `Viewport` only.
- No production application calls to `getSecondScale()` or `SecondScale`, no XML second-scale
  configuration, and no reflection API use were found. Internal vendored renderer/series calls to
  `getSecondScale()` were distinguished and are not application entry points.
- `SecondScale` initializes `mYAxisBoundsManual` to `true`; the only setter is commented out, so
  the `GridLabelRenderer` non-manual throw cannot be selected through its normal public API.
- No production GraphView code was changed. The focused proof is evidence about source
  reachability, not a runtime crash reproduction. Task 3.6 retains the separate GraphView lint
  baseline work.
- Verification: focused proof 3/3, full `testDebugUnitTest` 39/39, `assembleDebug`, and API 36
  `connectedDebugAndroidTest` 7/7 passed. `lintDebug` remains 5 errors, 121 warnings, 0 fatal.
