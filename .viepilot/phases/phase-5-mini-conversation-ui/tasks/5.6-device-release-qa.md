# Task 5.6 — Collect device and release evidence

## Source

`ENH-019`; 2026-09-20 audit proved host build and 9 unit tests but no connected device or end-to-end verification, and the release APK was unsigned.

## Paths

```text
app/src/androidTest/
app/src/test/
app/build.gradle
README.md
.viepilot/phases/phase-5-mini-conversation-ui/SPEC.md
```

## Plan

Run `testDebugUnitTest`, blocking lint, debug/release builds and `connectedDebugAndroidTest`. Record device/API/OS results for API 23, 31, 34 and 36; use a physical two-phone Bluetooth pair for Conversation mode. Exercise valid/invalid credential import, permission grant/deny/revoke, cloud failures, translation/TTS, WalkieTalkie, background/foreground, screen lock, process recreation and backup/restore. Verify accessibility/visual baselines and inspect a signed release artifact created with an external keystore. Document any untested path as a release blocker, not a pass.

## Done when

- Test matrix and known limitations are recorded with evidence and no unresolved High/Critical issue.
- Signed artifact installs, launches and completes core tasks on the supported matrix.
- Version and versionCode are selected/incremented only for the actual release; human release sign-off is recorded.
