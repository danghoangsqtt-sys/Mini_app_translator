package nie.translator.rtranslatordevedition.voice_translation.engines;

/** Identifies an explicitly selected engine family; it never selects a default. */
public enum EngineType {
    ON_DEVICE,
    LEGACY_CLOUD
}
