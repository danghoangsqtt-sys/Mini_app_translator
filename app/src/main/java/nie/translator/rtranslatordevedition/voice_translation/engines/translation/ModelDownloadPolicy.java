package nie.translator.rtranslatordevedition.voice_translation.engines.translation;

/** User-selected network constraint for an explicit translation-model download. */
public enum ModelDownloadPolicy {
    WIFI_ONLY,
    ANY_NETWORK
}
