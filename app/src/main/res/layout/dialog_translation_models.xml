<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingTop="8dp"
    android:paddingEnd="24dp"
    android:paddingBottom="8dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/translation_models_disclosure"
        android:textColor="@color/colorOnSurface"
        android:textSize="14sp" />

    <RadioGroup
        android:id="@+id/translation_models_network_policy"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:orientation="vertical">

        <RadioButton
            android:id="@+id/translation_models_wifi_only"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:checked="true"
            android:text="@string/translation_models_wifi_only" />

        <RadioButton
            android:id="@+id/translation_models_any_network"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="@string/translation_models_any_network" />
    </RadioGroup>

    <TextView
        android:id="@+id/translation_models_error"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:textColor="@color/colorError"
        android:visibility="gone" />

    <ProgressBar
        android:id="@+id/translation_models_loading"
        style="?android:attr/progressBarStyle"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="center_horizontal"
        android:layout_margin="16dp"
        android:indeterminate="true" />

    <ListView
        android:id="@+id/translation_models_list"
        android:layout_width="match_parent"
        android:layout_height="320dp"
        android:divider="@android:color/transparent"
        android:dividerHeight="4dp" />
</LinearLayout>
